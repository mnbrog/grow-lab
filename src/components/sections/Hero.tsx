import React from 'react';
import HeroSection from './HeroSection';
import StyledSection from './StyledSection';

const Hero = () => (
  <StyledSection>
    <HeroSection />
  </StyledSection>
);

export default Hero;
